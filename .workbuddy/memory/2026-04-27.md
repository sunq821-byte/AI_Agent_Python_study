# 2026-04-27 工作日志

## practice05 技能动态加载客户端

- 创建 `.agents/skills/notice/SKILL.md`，定义通知撰写技能（禁止以"通知"开头，需加部门前缀）
- 开发 `practice05/skill_client.py`，实现 `list_available_skills()`、`load_skill_content()` 两个核心函数
- 修复问题：
  - `.env` 文件中 `MODEL` 误写为 `ODEL`，变量名前有多余空格，已修正
  - LM Studio 使用 qwen3.5-9b 模型，多 system role 会导致第二轮返回空响应，改为 user role 注入技能内容
  - 流式解析加入 `<think>` 标签过滤，避免 thinking 内容被打印
  - 添加 `extra_body: {enable_thinking: false}` 禁用 thinking 模式
- Flake8 检查通过（0 错误）
- 两个测试全部通过：未说部门→"XX部通知"，指定销售部→"销售部通知"
- 更新 README.md，添加 practice05 使用说明和测试结果表格

## practice05 v2：skill_client_v2.py（Markdown 导出）

- 在 skill_client.py 基础上新增 `save_as_markdown(content, filename)` 工具
- 通知生成后，LLM 自动调用 save_as_markdown，保存至项目根目录 `notices/` 文件夹
- 文件结构：一级标题 + 正文 + 分隔线 + 生成时间戳
- 修复：LLM 传入 content 首行可能带 `#` 前缀，提取标题时先去除再套 h1，避免 `# # 标题` 重复
- Flake8 零错误，测试通过，文件正确生成于 notices/五一放假通知.md


- qwen3.5-9b 在 LM Studio 中对多 system message 不友好，第二轮返回空 content；改为 user role 传递工具结果可解决
- `.env` 文件里变量名前多余空格会导致 `load_env()` 解析失败（key 带空格），需注意
