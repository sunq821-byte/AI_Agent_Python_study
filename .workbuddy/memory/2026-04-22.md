# 2026-04-22 工作日志

## Pandoc + xelatex 实验报告转 PDF
- 发现系统已安装 TeX Live 2026（xelatex），可直接用 pandoc + xelatex 转 PDF
- 修改 `实验报告.md`：在开头添加 `\customcover` + `\newpage` 封面命令
- 重写 `pandoc_cn_style.tex`：添加 tikz 实现右上角 logo 定位，封面居中信息表格（课程名称、指导教师、学院、年级专业、班级、学号、姓名）
- 用 `cmd /c` 包裹 pandoc 命令解决 PowerShell 吞 stderr 的问题
- 替换 Markdown 中的 ✅ emoji 为 `[x]` 避免 SimSun 缺字符警告
- 最终命令：`pandoc 实验报告.md -o 实验报告.pdf --pdf-engine=xelatex -V mainfont=SimSun -V CJKmainfont=SimSun -H pandoc_cn_style.tex --resource-path=.`
- PDF 生成成功，139KB，零错误零警告
